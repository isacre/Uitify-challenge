import TextWithLabel from "./textInputWithLabel/textInput";
import SelectWithLabel from "./selectInputWithLabel/selectInput";
import CheckboxWithLabel from "./checkboxWithLabel/CheckboxComponent";

const Inputs = {
  TextWithLabel,
  SelectWithLabel,
  CheckboxWithLabel,
};

export default Inputs;
